const EN = 'en';
const RU = 'ru';

const SUPPORTED_LOCALES = [EN, RU];

const LABEL = {
  [EN]: 'English',
  [RU]: 'Русский',
};

const SCOPE = {
  NOT_FOUND: 'not-found',
};

export default {
  EN,
  RU,
  SUPPORTED_LOCALES,
  LABEL,
  SCOPE,
};
